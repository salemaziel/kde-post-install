const vscode = require('vscode');
const assert = require('assert');

const determinePath = require('../../src/utils/paths');

suite('paths', () => {
  test('it should resolve the root folder', () => {
    assert(1, 1);
  })
});